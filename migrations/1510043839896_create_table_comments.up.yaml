- args:
    sql: CREATE TABLE "comments"(id serial NOT NULL, article_id integer NOT NULL,
      comment text NOT NULL, PRIMARY KEY (id) )
  type: run_sql
- args:
    name: comments
  type: add_existing_table_or_view
